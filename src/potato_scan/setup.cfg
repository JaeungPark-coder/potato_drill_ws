[develop]
script-dir=$base/lib/potato_scan
[install]
install-scripts=$base/lib/potato_scan

[tool:pytest]
testpaths = test
markers =
    slow: takes seconds, not milliseconds (the coverage sweeps). Skip with -m "not slow"
